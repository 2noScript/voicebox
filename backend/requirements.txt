# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# ML models (MLX on Apple Silicon)
transformers>=4.36.0,<=4.57.6
huggingface_hub>=0.20.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0,<2.0
numba>=0.60.0,<0.61.0
pedalboard>=0.9.0
pyloudnorm

# HTTP client
httpx>=0.27.0

# MCP server (Model Context Protocol) — lets local AI agents call
# voicebox.speak / .transcribe / .list_captures / .list_profiles
fastmcp>=3.0,<4.0
sse-starlette>=2.0

# Utilities
python-multipart>=0.0.6
Pillow>=10.0.0
